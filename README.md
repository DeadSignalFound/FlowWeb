# FlowWeb

A Vite-powered single-page **Flow** home screen with an iOS-inspired, Dynamic Island-style animation.

## Local testing

```bash
npm install
npm run dev
```

Then open the local URL shown by Vite (typically `http://localhost:5173`).

## Build

```bash
npm run build
npm run preview
```
